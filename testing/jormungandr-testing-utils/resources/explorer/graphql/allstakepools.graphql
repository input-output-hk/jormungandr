query AllStakePools($first: Int!){ 
    mainTip {
        allStakePools(first: $first) {
            edges{
                node {
                    id
                }
            }
            totalCount
        }
    }
}